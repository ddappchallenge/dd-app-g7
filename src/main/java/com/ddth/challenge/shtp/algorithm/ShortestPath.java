package com.ddth.challenge.shtp.algorithm;

import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

import com.ddth.challenge.shtp.model.Edges;
import com.ddth.challenge.shtp.model.Nodes;

import org.json.JSONArray;
import org.json.JSONObject;
import org.springframework.boot.json.JsonParser;



public class ShortestPath {

    public void graph(ArrayList nodesList, Edges edges) {
        Nodes node = new Nodes();
        List<Nodes> nodes = new ArrayList<Nodes>();
        for (int i = 0; i < nodesList.size(); i++) {
            // node.setNode(nodesList.get(i));
        }
        
    }
}

